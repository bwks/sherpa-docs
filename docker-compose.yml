services:
  mkdocs:
    build:
      context: .
      dockerfile: Dockerfile
      network: host
    container_name: sherpa-docs
    ports:
      - "8888:8000"
    volumes:
      - ./:/docs
    stdin_open: true
    tty: true
